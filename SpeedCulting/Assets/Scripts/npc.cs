using UnityEngine;
using System.Collections;

public class npc : MonoBehaviour {

    public GameObject profile;

    int affection = 0;
    int suspicion = 0;
    
    // ask: [buttonID, affection number]
    // bribe: [buttonID, affection number]
 
    // ask: [0, -20]

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	    
	}
}
